# Demo data

Place the demo video here as:

`data/Dji_0079comp.mp4`

The supplied SRT file is already included as `data/DJI_0079.SRT`.

The current demo video is about 180 MB, which exceeds GitHub's normal 100 MB
single-file limit. Recommended options are:

1. submit the video separately together with the code,
2. use Git LFS, or
3. set the `DRONE_WASTE_VIDEO` environment variable to an external local path.

No source-code path changes are required.
